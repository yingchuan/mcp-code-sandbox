# Docker interpreter configuration
INTERPRETER_TYPE=docker

# Docker image settings
DOCKER_IMAGE_NAME=yingchuan/devenv:latest

# Optional: Custom workspace mount path
# DOCKER_WORKSPACE_MOUNT=/path/to/workspace

# Optional: Custom container name prefix
# DOCKER_CONTAINER_NAME_PREFIX=mcp-sandbox